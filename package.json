{
  "name": "@blessnet/config",
  "version": "0.1.0",
  "description": "SDK for BlessNet chains, contracts, and ABIs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "blessnet",
    "viem",
    "chains",
    "contracts",
    "abis",
    "sdk"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "viem": ">=1.0.0"
  }
}
